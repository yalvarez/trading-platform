[pytest]
asyncio_mode = auto

# Tests that require Docker services (psycopg2, mt5linux) are tagged `integration`.
# Run locally with:  pytest -m "not integration"
# Run all with:      pytest -m ""
markers =
    integration: requires Docker services (psycopg2, mt5linux, Redis)
    unit: runs without any external service
